<header class="bg-black text-white flex justify-between items-center">
  <h1 class="p-3 ">{{ shop.name }}</h1>
  <div class="flex gap-3">
    {% for link in linklists.main-menu.links -%}
      <a class="hover:text-red-500 {% if link.active %} text-red-500 {% endif %}" href="{{ link.url }}">{{ link.title }}</a>
    {%- endfor %}
  </div>
  <div>
    <p>Seach</p>
  </div>
</header>